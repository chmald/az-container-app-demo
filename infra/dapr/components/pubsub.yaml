apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: pubsub
spec:
  type: pubsub.redis
  version: v1
  metadata:
  - name: redisHost
    value: "{redis-host}"
  - name: redisPassword
    value: "{redis-password}"
  - name: enableTLS
    value: "true"
scopes:
- backend-service
