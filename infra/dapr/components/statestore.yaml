apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.redis
  version: v1
  metadata:
  - name: redisHost
    value: "{redis-host}"
  - name: redisPassword
    value: "{redis-password}"
  - name: enableTLS
    value: "true"
scopes:
- backend-service
